fullnameOverride: flight-service

image:
  repository: ""
  tag: "latest"

service:
  port: 8060

env:
  - name: DATABASE_URL
    value: "postgresql://postgres:postgres@postgres:5432/flights"

command:
  - uvicorn
  - services.flight_service.main:app
  - --host
  - 0.0.0.0
  - --port
  - "8060"
